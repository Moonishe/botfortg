"""Service layer — бизнес-логика между хендлерами и репозиториями.

Правила:
- Сервис НЕ знает про aiogram/Telegram (никаких Message, CallbackQuery).
- Сервис принимает сырые типы (int, str, dict) и возвращает dict/None.
- Сервис сам управляет сессиями БД через get_session().
- Ошибки логирует, НЕ пробрасывает исключения в хендлеры (возвращает None/False).
"""
